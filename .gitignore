# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml

# Android 签名（绝不能提交）
android/key.properties
*.jks
*.keystore

# 产出与临时
out/tmp/
out/shots/
*.log

# 大体积可再生产物（由 p0_rotate.py / p0_finalize_v1.py 等生成，不入库）
# 注意：out/ 下的报告（GATE_*.md、REVIEW_*.md、*_report.md）仍应提交，故不整体忽略 out/
out/P0_anchors/
out/GATE_ort/
out/P0_alpha_scan/
# qa-batch 指纹自测的沙箱：里面是 lib/ 与 test/ 的**完整副本**。
# 不忽略的话，一次 `git add -A` 就会把整个源码树复制一份提交进去。
out/P0_selftest_tmp/
# ml-porting 的瞳孔目视证据（overlay 图，32MB，可再生产物）
native/bench/out/

# 大文件：模型权重按需决定是否提交
# assets/models/*.onnx

# Python 字节码（曾在 5 处被误提交）
__pycache__/
*.pyc

# IDE / OS
.idea/
.vscode/
Thumbs.db
desktop.ini

# env-setup: 参考环境（仅开发机，不进 App，不提交）
.venv_ref/
.ref_hivision/
out/tmp_*/

# ORT 官方二进制留档（复现用，体量大，只留本地）
native/vendor/ort-bin/

# BiRefNet 量化管线的生成物（300MB，build_birefnet_model.py 可再生产，只留本地）
native/quantize/out/
native/quantize/src/
