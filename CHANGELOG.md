# 更新日志

## v1.4.0（双档抠图 + 手机布局）

- 双模型抠图档位：区域 A 新增黄铜拨杆「快速 / 精细（慢）」——
  快速档（MODNet）数秒出图，日常使用；精细档（BiRefNet）发丝级质量、
  小尺寸证件照也能抠稳，耗时约十倍，按需切换。档位换图不重置
- 抠图运行时升级（ONNX Runtime 1.29）+ 自适应线程：同一模型比 v1.3.3 快约 16%
- 手机布局：保存按钮压缩为紧凑尺寸，原图框选区域显著加大（占屏 45%→51%），
  拖框调角度更好操作；桌面版布局不变

## v1.3.3（BiRefNet 抠图换档 + 实时预览）

- 抠图模型更换为 BiRefNet-lite（MIT 许可）：发丝、眼镜腿等细节显著更干净；
  修复旧模型对**小尺寸证件照原图**彻底失效（人消失只剩碎片）的致命场景
- 代价说明：模型 67MB（App 体积相应变大），首次抠图约需十几秒（每张原图
  只跑一次，之后换底/调框/调角不受影响）——质量优先的取舍，后续版本提速
- 实时预览：拖框/调角度期间候选条即时跟手（对候选图直接做旋转缩放变换），
  停手后自动替换为全精度结果；任何时候点保存都是当前几何的高清版本

## v1.3.2（发丝边缘质量）

- 抠图模型输入分辨率 512 → 1024：发丝细节翻倍，红边/锯齿进一步收敛
- 发丝边缘恢复软过渡：不再把半透明边缘带二值化硬切，头发丝按真实覆盖率
  与新底色柔和合成，换底后不再有脏色硬边
- 渐进式预览：拖框/调角度时候选条 120ms 出草稿跟手，停手后自动换成全精度；
  保存时一律按当前几何全精度重合成，任何时候点保存都是高清结果
- （v1.3.1 为内部验证版，未发布）

## v1.3.0（手动微调 + 抠图质量）

- 手动角度微调：区域 A 新增木制角度表盘（±30°，左右 0.5° 微调钮 + 回正钮）；
  微调时照片不动、只有框选框转，所见即所得
- 自动摆正收窄：只在照片方向明显不对（倾斜超过 30°）时才自动转，普通照片不再被越俎代庖
- 抠图边缘去锯齿：512 蒙版放大前先去斑 + 平滑，发丝轮廓由阶梯变连续曲线
- 关于页入口：改为左上角"木照"铭牌短按（此前为长按且无提示，用户发现不了）
- Windows 绿色版正式上架：免安装、解压即用，发布页可直接下载

## v1.2.0（美术升级）

- App 图标重做：木纹 + 黄铜椭圆框吉祥物头像（AI 生成日漫风）
- Windows：窗口宽高比锁定 1080:2220（含最大化 letterbox）、最小尺寸、
  图标更新；取图/保存适配桌面交互
- 官方演示素材全面换为日漫风吉祥物"木木"
- 发布页：英文版上线、仓库链接提上顶栏、轮播居中、APK 直链下载

## v1.0.0（首发）

### Android
- 端侧 AI 抠图（MODNet int8，7.2MB 模型）+ YuNet 人脸检测
- 7 种证件规格 × 6 种底色，像素级精确 + 300 DPI
- 去色边换底（纯色/渐变），杜绝边缘溢色
- 拖拽框选（8 控制点、比例锁定、边界钳制）
- 复古木制工作台 UI（程序化材质，零图片资源）
- 完全离线：无网络权限、无统计、无广告
- 中文界面，含关于页（作者与隐私承诺）

### 桌面（Windows）· 预览
- 同一 Flutter 代码库的 Windows 移植预览
- 端侧推理与 Android 数值一致（黄金集 alpha 99.87% 逐字节同）
- 取图/保存适配桌面交互（文件对话框）

### 质量
- 黄金集 IoU 最低 0.9673，边缘带 MAE ≤ 0.1085
- 80 项真实数据回归：0 崩溃 0 ANR，人像 14/14，非人像 100% 优雅失败
- 59 例对抗测试：0 崩溃 0 数据损坏
- 内存：真机稳态无泄漏（20 轮斜率 ±7MB 内）
