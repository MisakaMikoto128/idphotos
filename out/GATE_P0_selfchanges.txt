# gatekeeper 本轮（G2B-P0）自报改动清单（滚动更新）。
# 这些文件是门禁自己的工具，由 gatekeeper 创建/修改；在此逐条声明，
# 供防作弊巡查豁免"越界写 tools/gate // test/gate"的判定，并在报告里接受复核。
# 声明外的任何 tools/gate、test/gate、docs/ACCEPTANCE.md、docs/RUBRIC.md
# 改动，一律按 ACCEPTANCE 防作弊条款 1/2 判 FAIL。
tools/gate/gate_P0.dart
tools/gate/anticheat.dart
tools/gate/p0_eyeline.py
tools/gate/p0_rigid_check.py
tools/gate/p0_overlap.py
tools/gate/p0_roll_probe_test.dart
tools/gate/rot_dir_check.dart
test/gate/p0_gate_judgment_test.dart
tools/gate/gate_G5B.dart
test/gate/anticheat_test.dart
tools/gate/sha256.dart
# 2026-09-17 追加（主会话裁定的两件 pre-tag 工作）：
#  · `provenance.dart` / `provenance_test.dart` —— 测量产出的代码来源绑定；
#  · `gate_common.dart` —— 补进清单：它在上一轮就被我改过（条款 5 的 kill 失败留痕），
#    却一直没登记。清单漏一条 = 下一轮会**误判我自己越界**，与本条链上其余缺陷同形。
tools/gate/provenance.dart
test/gate/provenance_test.dart
tools/gate/gate_common.dart
# 2026-09-17 追加（主会话裁定的 pre-tag 收尾三件）：
#  · gate_P0.dart —— 真值数值叶基线的 `evaluatedState` 排除集（常数 + 两侧同用 +
#    每轮打印被排除的路径清单）+ 指纹域文件清单渲染 + 「叶哈希未变 ≠ 证据有效」附注；
#  · p0_gate_judgment_test.dart —— 上述排除集的正负对照（P/Q/R/R2/S/T/U/V）；
#  · provenance_test.dart —— 域清单渲染（不只个数）与 git 不可用时的数目口径。
# 三个文件都在上面已列出，这里只是记明**本轮为什么动它们**。
# 2026-09-17 第二批：同上三个文件，另加本文件与 docs/PITFALLS.md。
#  · gate_P0.dart —— 基线文件进 kPinnedJudgmentInputs；基线重建轮整轮作废；
#    TruthDrift.describe() 在重建时**不再**写"本轮不判 FAIL"（那句话就是绕过路径的门）；
#  · p0_gate_judgment_test.dart —— 测试 W（删基线买不到免疫）、X（锚在受钉集里）；
#  · docs/PITFALLS.md —— 追加一条（只追加，未删改他人条目）。
# 2026-09-17 第三批（shell 参数事故）：新增登记以下文件 —— 它们此前从未被改过，
# 因此不在清单里；本轮修 `runInShell` 事故时动了它们，必须登记，
# 否则下一轮会把我自己判成越界。
tools/gate/device_harness_common.dart
tools/gate/gate_G4.dart
tools/gate/gate_G5.dart
# 2026-09-17 第四批：补登两个 `integration_test/` 文件。
# 它们在本阶段早期就被我改过（条款 5：`finally` 里的 dispose 空 catch 改为
# 收集成 `disposeErrors`），但一直没进清单 —— 与 `gate_common.dart` 那次同形。
# 清单漏一条的后果不是"少写一行"：它会让下一轮的哈希差异栏里出现**无法解释的改动**，
# 而"无法解释"与"有问题"在报告里看起来是一样的。
integration_test/g4_memcheck_test.dart
integration_test/g4_spotcheck_test.dart
